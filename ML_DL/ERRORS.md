[[Tensorflow]]
1. **TensorFlow tensors _can_ be multiplied with NumPy arrays** _BUT only if the dtypes match._  
Otherwise TensorFlow will throw the dtype error (like the one you saw).
i faced a error while multiplying a np float64 with tf float32



