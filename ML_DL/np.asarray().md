[[Numpy]]
converts any - **sequence-like object** (list, tuple, etc.) into a **NumPy array**.
    
- If the input is already a NumPy array, it **doesn’t copy it** (unlike `np.array()` which always copies by default unless you specify).